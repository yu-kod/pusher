# pusher-table 開発ルール

## 開発の進め方

実装を始める前に、必ず `.claude/skills/coding-standards.md` を読み、その内容に従って実装を進めること。
このファイルはコーディング規約のプロフェッショナルとして、TDD の進め方・テストの書き方・設計判断についてガイドする。

### コーディング規約を参照するタイミング

- **新しい機能の実装を始める前** — スキルを読み、最初に書くテストの方針を決める
- **設計判断に迷った時** — テスタビリティの観点から評価する
- **リファクタリングの前** — 改善方針を確認する

### 参照の手順

1. `.claude/skills/coding-standards.md` を読む
2. 該当するセクション（pusher-table 固有のルール、TDD、テスト、バックエンド、フロントエンド等）のルールを確認する
3. ルールに沿った方針を立ててから実装に入る
4. **ルールに従えない・判断できない場合はスキルファイル自体が不十分** — スキルの修正を優先する

### 絶対のルール

- 失敗するテストなしにプロダクションコードを書かない
- テストの実行確認を省略しない（Red で失敗、Green で成功を毎回確認）
- コーディング規約の参照を省略しない
- ゲームエンジン（`backend/src/game/`）に I/O を持ち込まない。乱数は `Rng` を引数で注入する
- 裏向き情報をクライアントへ返さない

---

## ゲームルール

ゲームのルールは `docs/spec.md` に定義されている。ゲームロジックを変更する前に必ず読むこと。
ルールの解釈に迷った場合は `docs/spec.md` の「ルール解釈メモ」を優先し、新たな解釈が必要ならそこに追記する。

---

## チケット管理（GitHub Issues）

作業の単位は **GitHub Issue**（チケット）とする。

### 基本ルール

- **Issue のないコードは書かない。** まず Issue を立て、そこに「何を・なぜ」を書いてから着手する
- 1つの Issue は **1つの論理的な変更** に対応させる。複数の無関係な変更を1枚にまとめない
- Issue のタイトルは日本語で、何が達成されるかが分かる形にする（例: `トラック結合機能（同時にかける2曲を1枠にまとめる）`）
- 着手時に Issue を自分にアサインする
- Issue は PR がマージされた時点でクローズする（PR 本文の `Closes #N` で自動クローズさせる）

### Issue とコミット／PR の紐付け

- コミットの subject 末尾に Issue 番号を `(#N)` の形で入れる
  ```
  feat: セットリスト別の表示回数ランキングページを追加 (#126)
  ```
- PR 本文に `Closes #N` を書き、マージで Issue が閉じるようにする
- 複数コミットに分かれる作業でも、各コミットに同じ `(#N)` を付けて追跡できる状態を保つ

---

## ブランチ戦略（トランクベース開発）

- **`main`** がトランク。常にデプロイ可能な状態を保つ
- 作業は **短命のフィーチャーブランチ** で行い、PR 経由で main にマージする
- ブランチ命名: `feature/xxx`, `fix/xxx`, `refactor/xxx`, `chore/xxx`
- 1つの PR は 1つの論理的な変更に対応させる（複数の無関係な変更を混ぜない）
- **main への直接プッシュは禁止。** CI が通った PR のみマージする
- マージ後のブランチは削除する

### コミットメッセージ

Conventional Commits の prefix ＋ 日本語の本文。

| prefix | 用途 |
|---|---|
| `feat:` | 機能追加 |
| `fix:` | バグ修正 |
| `refactor:` | 挙動を変えない内部改善 |
| `style:` | フォーマットのみ（Prettier 等） |
| `chore:` | ビルド・設定・依存関係 |
| `revert:` | 変更の取り消し |
| `wip:` | 未完成の途中コミット（PR 内でのみ許容） |

---

## PRマージの流れ

実装が完了したら `.claude/skills/pr-merge.md` のスキルに従ってPRを作成する。

1. フィーチャーブランチにコミット & プッシュ
2. GitHub MCP ツールでPR作成（本文に `Closes #N`）
3. Auto-merge を有効化（CIパスで自動マージ）

**このスキルを省略しない。** main への直接プッシュは禁止。

---

## マージ後の動作確認フロー

変更の種類によって確認フローが異なる。

### フロントエンド（UI確認が必要な変更）

PRマージ後、**ユーザーに手動確認を依頼する**。自分で完了と判断しない。

1. PRがマージされたことを報告する
2. ユーザーに確認してほしい手順を具体的に伝える（画面遷移、操作手順、確認ポイント）
3. ユーザーの確認結果を待つ
4. 問題があれば修正、なければ次のタスクに進む

### バックエンド / API / インフラ

テストに加えて、必要に応じて curl 等で実際のエンドポイントを叩いて動作確認する。ユーザーへの確認依頼は不要。
